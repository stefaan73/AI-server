
# AI Gevel Kleur Tool

## Installatie
1. Installeer dependencies: `pip install -r requirements.txt`
2. Start de server: `python app.py`

## Gebruik
Stuur een POST-request naar `/upload` met:
- `image`: de gevelafbeelding
- `color`: hex-kleurcode (zoals `#FF0000`)
